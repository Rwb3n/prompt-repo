Activate the minimal structure behind every concept. 
For each, extract its essence, dependencies, compression function, problem domain, and compositional power. 
Reconstruct the full field through sparse, linked primitives.
